# 项目协作执行规则

本文件适用于项目根目录及其全部子目录，所有人类与 AI 执行者均须遵守。

## 1. 文档与架构优先

- 实现必须符合 `README.md`、`docs/` 和已接受 ADR。
- 改变产品范围时更新产品文档；改变架构、安全边界、权限、数据模型、分支语义或外部服务时新增或更新 ADR。
- 未经 PO 批准，不扩大浏览器权限、不外发用户数据、不启用遥测或远程 LLM、不执行高风险状态变更。

## 2. 修改日志

- 每个实际落地的项目修改都要同步更新 `CHANGELOG.md`，记录目的、步骤、涉及文件、实际验证和遗留事项。
- 不把计划写成已完成，不伪造测试、评审或发布结果。
- 临时分析且未改变受版本控制文件的工作不必单独提交；一旦形成项目决策、文档、源码、配置或测试变化，就必须记录。

## 3. Git 可回溯性

- 每个完成并通过验证的独立修改形成一个小而完整的原子提交。
- 提交信息使用清晰的约定式前缀，例如 `docs:`、`feat:`、`fix:`、`test:`、`refactor:`、`chore:`。
- 提交前检查变更范围、敏感信息、临时文件和必要验证；不得提交令牌、Cookie、API key、私人聊天正文或未脱敏 fixture。
- 提交完成后推送到已配置的 GitHub 远端，并验证本地分支与上游同步。
- 推送失败时保留本地提交、记录失败原因并明确告知 PO；不得宣称已经同步。
- 未经 PO 明确授权，禁止强制推送、重写已发布历史、删除远端分支或覆盖他人提交。
- 不使用破坏性 Git 命令清除未确认的用户修改。

## 4. 推荐执行顺序

1. 确认任务卡、范围、主责、复核和验收标准。
2. 检查当前分支、工作区和远端同步状态。
3. 完成限定范围内的修改。
4. 执行与风险匹配的测试、检查或文档验证。
5. 更新 `CHANGELOG.md`。
6. 审查最终 diff，确认没有秘密、真实私人内容或无关修改。
7. 创建原子提交并推送。
8. 验证上游分支包含新提交，再报告完成。

## 5. 分支与协作

- 文档和低风险维护可在 PO 允许的当前分支完成。
- 功能开发、中高风险修改或多人并行修改优先使用独立功能分支，并通过审查后合并。
- 同一文件在同一时间只安排一个主责执行者修改；其他角色以只读评审方式协作。
- 中高风险变更必须由非作者复核，具体门禁遵循 `docs/09-team-workflow.md`。

## 6. 完成判定

项目修改只有同时满足以下条件才可报告为“已完成”：

- 交付物符合任务范围与验收标准。
- 必要检查或测试已实际通过。
- `CHANGELOG.md` 已更新。
- 必要 ADR 与评审已完成。
- Git 提交已创建。
- 提交已推送，且本地分支与 GitHub 上游同步。

