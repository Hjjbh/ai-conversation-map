# 阶段 0 脱敏测试材料

本目录只保存经过白名单输出、自动敏感扫描和 QSR 人工复核的阶段 0 材料。

## 目录

- `observations/`：只读探针生成的脱敏结构观察结果。

观察 JSON 不是原始 DOM fixture，不包含正文、真实 ID、URL、属性值或 DOM 片段。它可用于复核计数、角色顺序和结构信号，但不能替代未来的最小化脱敏 DOM fixture，也不能单独证明选择器跨版本稳定。

任何新增材料必须具有独立证据编号、SHA-256、授权引用、扫描结果和非作者复核结论。
